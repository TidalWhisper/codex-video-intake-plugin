# Windows CMD 安装

```cmd
cd /d E:\Codex-Plugin\codex-video-intake-plugin
verify_package.cmd
install_personal_plugin.cmd
```

然后关闭所有 Codex CLI 窗口，重新执行：

```cmd
codex
```

进入 `/plugins`，安装或启用：

```text
Codex Video Pipeline
```

推荐入口：

```text
$video-production-pipeline
```
