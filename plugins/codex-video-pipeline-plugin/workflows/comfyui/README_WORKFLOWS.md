# ComfyUI workflow 放置目录

这里暂时只放占位说明，不内置可运行 workflow。

后续你在本机 ComfyUI 中导出 API 格式工作流后，放到这里：

```text
txt2img_keyframe.workflow_api.json
i2v_ltx.workflow_api.json
indextts2.workflow_api.json
music_generation.workflow_api.json
```

注意：必须是 API 格式 workflow，不是普通 UI workflow。
